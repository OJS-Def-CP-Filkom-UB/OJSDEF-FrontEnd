// Legacy types file — replaced by types/api.ts
// This stub prevents import errors during migration; Agent 2 will update remaining importers
export type OJSScanType = never
export type PluginStatus = never
export type RiskLevel = never
export type UserRole = never
export interface OJSTarget { id: string; [key: string]: unknown }
export interface ScanFinding { id: string; [key: string]: unknown }
export interface ActionPlanStep { [key: string]: unknown }
export interface ScanSession { id: string; [key: string]: unknown }
